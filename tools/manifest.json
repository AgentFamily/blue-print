[
  {
    "name": "system_packet_validator",
    "description": "Validates and extracts SYSTEM_PACKET JSON blocks from assistant outputs.",
    "entrypoint": "tools.system_packet_validator.main:run",
    "args_schema": {
      "type": "object",
      "properties": {
        "text": { "type": "string" }
      },
      "required": ["text"]
    },
    "examples": [
      { "text": "Here is response:\n```json\n{\"SYSTEM_PACKET\": {...}}\n```" }
    ],
    "permissions": { "filesystem": "none", "network": "none", "secrets": "none" }
  }
]
